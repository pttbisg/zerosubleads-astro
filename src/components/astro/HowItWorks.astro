---
import { Icon } from 'astro-icon/components';
---

<section id="how-it-works" class="bg-white py-24 sm:py-32 relative overflow-hidden">
  <!-- Background Decor -->
  <div class="absolute inset-0 bg-[radial-gradient(#e5e7eb_1px,transparent_1px)] [background-size:16px_16px] [mask-image:radial-gradient(ellipse_50%_50%_at_50%_50%,#000_70%,transparent_100%)] opacity-50 pointer-events-none"></div>

  <div class="mx-auto max-w-7xl px-6 lg:px-8 relative z-10">
    <div class="mx-auto max-w-2xl text-center mb-20">
      <h2 class="text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl">
        Three steps. Under a minute.
      </h2>
      <p class="mt-4 text-lg text-gray-600">
        A linear workflow designed for speed and accuracy.
      </p>
    </div>

    <div class="relative">
      <!-- Connector Line Container (Desktop) -->
      <div class="hidden md:block absolute top-[105px] left-[16%] right-[16%] h-0.5 z-0 overflow-hidden">
         <!-- Flowing Dashed Line -->
         <div class="absolute inset-0 bg-[linear-gradient(90deg,theme(colors.gray.300)_50%,transparent_50%)] bg-[length:12px_100%] animate-flow"></div>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-12">

        <!-- Step 1: Search -->
        <div class="relative group">
          <!-- Card -->
          <div class="relative z-10 bg-white rounded-2xl border border-gray-200 p-8 shadow-sm transition-all duration-300 hover:shadow-xl hover:border-primary-500 hover:-translate-y-1">
            <!-- Number Badge -->
            <div class="absolute -top-4 left-8 bg-gray-900 text-white text-sm font-mono py-1 px-3 rounded shadow-lg">
              STEP 01
            </div>

            <!-- Icon -->
            <div class="mb-6 group-hover:scale-110 transition-transform duration-300">
               <Icon name="lucide:search" class="h-10 w-10 text-gray-900 stroke-[1.5]" />
            </div>

            <h3 class="text-xl font-bold text-gray-900 mb-3">Define</h3>
            <p class="text-gray-600 text-sm leading-relaxed mb-6">
              Input your criteria using natural language. "Marketing Directors in Chicago at Series B companies."
            </p>

            <!-- Mock UI Element -->
            <div class="w-full bg-gray-50 rounded border border-gray-100 p-2 flex items-center gap-2">
               <div class="h-2 w-2 rounded-full bg-red-400"></div>
               <div class="h-1.5 w-2/3 bg-gray-200 rounded-full"></div>
            </div>
          </div>

          <!-- Connector Dot (Desktop) -->
          <div class="hidden md:block absolute top-[100px] -right-1.5 w-3 h-3 bg-white border-2 border-gray-200 rounded-full z-20 group-hover:border-primary-500 group-hover:scale-125 transition-all"></div>
        </div>

        <!-- Step 2: Preview -->
        <div class="relative group">
           <!-- Card -->
          <div class="relative z-10 bg-white rounded-2xl border border-gray-200 p-8 shadow-sm transition-all duration-300 hover:shadow-xl hover:border-primary-500 hover:-translate-y-1">
            <div class="absolute -top-4 left-8 bg-white border border-gray-200 text-gray-500 text-sm font-mono py-1 px-3 rounded shadow-sm">
              STEP 02
            </div>

            <div class="mb-6 group-hover:scale-110 transition-transform duration-300">
               <Icon name="lucide:database" class="h-10 w-10 text-gray-900 stroke-[1.5]" />
            </div>

            <h3 class="text-xl font-bold text-gray-900 mb-3">Review</h3>
            <p class="text-gray-600 text-sm leading-relaxed mb-6">
               See full LinkedIn profiles before you spend a cent. We verify email validity in real-time.
            </p>

            <!-- Mock UI Element -->
            <div class="flex gap-2 justify-center">
               <div class="h-8 w-8 rounded-full bg-gray-100 border border-white shadow-sm -ml-2 first:ml-0"></div>
               <div class="h-8 w-8 rounded-full bg-gray-100 border border-white shadow-sm -ml-2"></div>
               <div class="h-8 w-8 rounded-full bg-gray-100 border border-white shadow-sm -ml-2 flex items-center justify-center text-[10px] text-gray-400 font-medium">+5</div>
            </div>
          </div>

           <!-- Connector Dot (Desktop) -->
          <div class="hidden md:block absolute top-[100px] -right-1.5 w-3 h-3 bg-white border-2 border-gray-200 rounded-full z-20 group-hover:border-primary-500 group-hover:scale-125 transition-all"></div>
          <div class="hidden md:block absolute top-[100px] -left-1.5 w-3 h-3 bg-white border-2 border-gray-200 rounded-full z-20"></div>
        </div>

        <!-- Step 3: Export -->
        <div class="relative group">
           <!-- Card -->
          <div class="relative z-10 bg-white rounded-2xl border border-gray-200 p-8 shadow-sm transition-all duration-300 hover:shadow-xl hover:border-primary-500 hover:-translate-y-1">
            <div class="absolute -top-4 left-8 bg-white border border-gray-200 text-gray-500 text-sm font-mono py-1 px-3 rounded shadow-sm">
              STEP 03
            </div>

            <div class="mb-6 group-hover:scale-110 transition-transform duration-300">
               <Icon name="lucide:download" class="h-10 w-10 text-gray-900 stroke-[1.5]" />
            </div>

            <h3 class="text-xl font-bold text-gray-900 mb-3">Extract</h3>
            <p class="text-gray-600 text-sm leading-relaxed mb-6">
               Download your list or sync to CRM. Only pay for verified leads. No wasted credits.
            </p>

            <!-- Mock UI Element -->
            <div class="w-full bg-green-50 rounded border border-green-100 p-2 flex items-center justify-center text-xs font-mono text-green-700 gap-2">
               <Icon name="lucide:shield-check" class="h-3 w-3" /> Export Complete
            </div>
          </div>

          <!-- Connector Dot (Desktop) -->
          <div class="hidden md:block absolute top-[100px] -left-1.5 w-3 h-3 bg-white border-2 border-gray-200 rounded-full z-20"></div>
        </div>

      </div>

       <!-- Mobile Connecting Line (Vertical) -->
       <div class="md:hidden absolute top-0 bottom-0 left-8 w-0.5 z-0 bg-[linear-gradient(180deg,theme(colors.gray.300)_50%,transparent_50%)] bg-[length:100%_12px] animate-flowVertical"></div>
    </div>

    <!-- Data Quality Banner -->
    <div class="mt-20 rounded-2xl bg-gray-50 p-8">
      <div class="flex flex-col md:flex-row gap-8 md:gap-0">

        <div class="flex-1 flex items-center gap-5 md:pr-8 md:border-r border-gray-200">
          <Icon name="lucide:refresh-cw" class="h-10 w-10 text-gray-900 stroke-[1.5]" />
          <div>
            <h4 class="font-bold text-gray-900 text-lg">Fresh Data</h4>
            <p class="text-gray-600 mt-1">LinkedIn data is refreshed every 6 weeks.</p>
          </div>
        </div>

        <div class="flex-1 flex items-center gap-5 md:pl-8">
          <Icon name="lucide:users" class="h-10 w-10 text-gray-900 stroke-[1.5]" />
          <div>
            <h4 class="font-bold text-gray-900 text-lg">Deep Coverage</h4>
            <p class="text-gray-600 mt-1">Covers profiles of everyone at companies &gt;7 people.</p>
          </div>
        </div>

      </div>
    </div>

  </div>
</section>
